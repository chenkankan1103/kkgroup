import discord
from discord.ext import commands
import json
import random
import traceback
import os
import aiohttp
from datetime import datetime
from .database import get_user, update_user

# AI API ???
AI_API_KEY = os.getenv("AI_API_KEY", "")
AI_API_URL = os.getenv("AI_API_URL", "")
AI_API_MODEL = os.getenv("AI_API_MODEL", "meta-llama/llama-4-scout-17b-16e-instruct")

# ?????? - Lv.0 ??Lv.6
LEVELS = {
    0: {
        "title": "??????",
        "actions": [
            {"name": "???", "risk": 0.1, "base_reward": 50, "success_rate": 0.95, "xp": 10}
        ],
        "salary": 25,
        "days_required": 0,
        "xp_required": 0,
        "role_id": 0,
        "salary_boost": 1.0,
        "xp_boost": 1.0,
        "description": "???????????"
    },
    1: {
        "title": "??????(???)",
        "actions": [
            {"name": "???", "risk": 0.1, "base_reward": 100, "success_rate": 0.9, "xp": 20}
        ],
        "salary": 150,
        "days_required": 7,
        "xp_required": 500,
        "role_id": int(os.getenv("ROLE_CAR", 0)),
        "salary_boost": 1.0,
        "xp_boost": 1.0,
        "description": "???????????"
    },
    2: {
        "title": "???????",
        "actions": [
            {"name": "???", "risk": 0.1, "base_reward": 100, "success_rate": 0.9, "xp": 20},
            {"name": "???", "risk": 0.2, "base_reward": 200, "success_rate": 0.85, "xp": 35},
            {"name": "???", "risk": 0.3, "base_reward": 300, "success_rate": 0.75, "xp": 50}
        ],
        "salary": 275,
        "days_required": 14,
        "xp_required": 1500,
        "role_id": int(os.getenv("ROLE_CHAT_WORKER", 0)),
        "salary_boost": 1.0,
        "xp_boost": 1.0,
        "description": "???????????"
    },
    3: {
        "title": "???/???",
        "actions": [
            {"name": "???", "risk": 0.1, "base_reward": 100, "success_rate": 0.9, "xp": 20},
            {"name": "???", "risk": 0.2, "base_reward": 200, "success_rate": 0.85, "xp": 35},
            {"name": "??????", "risk": 0.25, "base_reward": 400, "success_rate": 0.8, "xp": 60},
            {"name": "???", "risk": 0.4, "base_reward": 600, "success_rate": 0.7, "xp": 80}
        ],
        "salary": 450,
        "days_required": 28,
        "xp_required": 4000,
        "role_id": int(os.getenv("ROLE_TEAM_LEAD", 0)),
        "salary_boost": 1.0,
        "xp_boost": 1.0,
        "description": "??????????????"
    },
    4: {
        "title": "??????",
        "actions": [
            {"name": "???", "risk": 0.1, "base_reward": 100, "success_rate": 0.9, "xp": 20},
            {"name": "???", "risk": 0.2, "base_reward": 350, "success_rate": 0.85, "xp": 45},
            {"name": "????", "risk": 0.3, "base_reward": 300, "success_rate": 0.75, "xp": 50},
            {"name": "?????", "risk": 0.5, "base_reward": 800, "success_rate": 0.65, "xp": 100},
            {"name": "??????", "risk": 0.6, "base_reward": 1000, "success_rate": 0.6, "xp": 120}
        ],
        "salary": 750,
        "days_required": 42,
        "xp_required": 8500,
        "role_id": int(os.getenv("ROLE_ACCOUNTING", 0)),
        "salary_boost": 1.1,
        "xp_boost": 1.0,
        "description": "?????????????????? +10%??"
    },
    5: {
        "title": "??????",
        "actions": [
            {"name": "???", "risk": 0.35, "base_reward": 700, "success_rate": 0.75, "xp": 90},
            {"name": "??????", "risk": 0.15, "base_reward": 400, "success_rate": 0.88, "xp": 55},
            {"name": "??????", "risk": 0.1, "base_reward": 300, "success_rate": 0.92, "xp": 40},
            {"name": "??????", "risk": 0.25, "base_reward": 500, "success_rate": 0.82, "xp": 70},
            {"name": "??????", "risk": 0.7, "base_reward": 1500, "success_rate": 0.5, "xp": 150}
        ],
        "salary": 1250,
        "days_required": 60,
        "xp_required": 18000,
        "role_id": int(os.getenv("ROLE_SCAM_ROOM", 0)),
        "salary_boost": 1.0,
        "xp_boost": 1.2,
        "description": "?????????????????????? +20%??"
    },
    6: {
        "title": "????????",
        "actions": [
            {"name": "???", "risk": 0.35, "base_reward": 700, "success_rate": 0.75, "xp": 90},
            {"name": "??????", "risk": 0.15, "base_reward": 400, "success_rate": 0.88, "xp": 55},
            {"name": "??????", "risk": 0.1, "base_reward": 300, "success_rate": 0.92, "xp": 40},
            {"name": "??????", "risk": 0.25, "base_reward": 500, "success_rate": 0.82, "xp": 70},
            {"name": "??????", "risk": 0.7, "base_reward": 1500, "success_rate": 0.5, "xp": 150}
        ],
        "salary": 2100,
        "days_required": 90,
        "xp_required": 40000,
        "role_id": int(os.getenv("ROLE_REGIONAL_DIRECTOR", 0)),
        "salary_boost": 1.0,
        "xp_boost": 1.0,
        "description": "?????????????????????????????????????"
    }
}

# ?????????? (Lv.0 ??????Lv.1-6 ???????7, 14, 28, 42, 60, 90 ??
REQUIRED_DAYS = [0, 7, 14, 28, 42, 60, 90]

def required_days_for_level(level):
    """?????????????????????"""
    try:
        if level < 0 or level >= len(REQUIRED_DAYS):
            return 999
        return REQUIRED_DAYS[level]
    except Exception as e:
        traceback.print_exc()
        return 999

def check_level_up(user):
    """??????????????????????? + ??????"""
    level = user.get('level', 0)
    if level >= 6:
        return False, None
    
    streak = user.get('streak', 0)
    xp = user.get('xp', 0)
    
    next_level = level + 1
    required_days = LEVELS[next_level]["days_required"]
    required_xp = LEVELS[next_level]["xp_required"]
    
    days_met = streak >= required_days
    xp_met = xp >= required_xp
    
    if days_met and xp_met:
        return True, {"days": required_days, "xp": required_xp}
    else:
        return False, {
            "days_met": days_met,
            "xp_met": xp_met,
            "required_days": required_days,
            "required_xp": required_xp,
            "current_days": streak,
            "current_xp": xp
        }

def create_progress_bar(current, total, length=10):
    """????????"""
    filled = int((current / total) * length) if total > 0 else 0
    filled = min(filled, length)
    bar = "?? * filled + "?? * (length - filled)
    percentage = int((current / total) * 100) if total > 0 else 0
    return f"{bar} {percentage}%"

def create_work_embed(user, user_obj):
    """???????????Embed"""
    try:
        level = user['level']
        level_info = LEVELS[level]
        
        colors = {0: 0x7f8c8d, 1: 0x95a5a6, 2: 0x3498db, 3: 0x9b59b6, 4: 0xe74c3c, 5: 0xf39c12, 6: 0xdaa520}
        embed = discord.Embed(
            title="?????????? ????????????",
            color=colors.get(level, 0x2f3136)
        )
        
        embed.add_field(
            name="?? ??????", 
            value=(
                f"**???**??user_obj.mention}\n"
                f"**???**??level_info['title']}\n"
                f"**???**??v.{level} ??
            ),
            inline=True
        )
        
        embed.add_field(
            name="?? ???????",
            value=(
                f"**???**??user['kkcoin']:,} KK??n"
                f"**???**??level_info['salary']:,} KK??n"
                f"**???**??user['xp']:,} XP"
            ),
            inline=True
        )
        
        embed.add_field(
            name="?? ???????",
            value=(
                f"**???**??user['streak']} ????\n"
                f"**???**??len(level_info['actions'])} ??n"
                f"**????*??? ?????
            ),
            inline=True
        )
        
        if level < 6:
            can_level_up, info = check_level_up(user)
            
            if can_level_up:
                embed.add_field(
                    name="?? ??????", 
                    value=(
                        "```diff\n"
                        "+ ????????????????????\n"
                        "+ ??????????????n"
                        "```"
                    ),
                    inline=False
                )
            else:
                days_progress = create_progress_bar(info['current_days'], info['required_days'])
                xp_progress = create_progress_bar(info['current_xp'], info['required_xp'])
                
                next_level_info = LEVELS[level + 1]
                salary_increase = next_level_info['salary'] - level_info['salary']
                
                progress_text = (
                    f"**????????next_level_info['title']}** (Lv.{level + 1})\n\n"
                    f"?? **??????**??info['current_days']}/{info['required_days']} ??n"
                    f"{days_progress}\n\n"
                    f"?? **??????**??info['current_xp']:,}/{info['required_xp']:,} XP\n"
                    f"{xp_progress}\n\n"
                    f"?? **?????????**??n"
                    f"???????? +{salary_increase:,} KK??n"
                    f"???????? +{(level + 1) * 300:,} KK??n"
                    f"???????????{len(next_level_info['actions'])} ??
                )
                
                embed.add_field(name="?? ??????", value=progress_text, inline=False)
        else:
            embed.add_field(
                name="?? ???????- ????????",
                value=(
                    "```yaml\n"
                    "??????????????????????\n"
                    "??????????????????????n"
                    "```"
                ),
                inline=False
            )
        
        # ???????????????
        actions_text = ""
        for action in level_info['actions']:
            risk_emoji = "??" if action['risk'] <= 0.2 else "??" if action['risk'] <= 0.4 else "??"
            actions_text += f"{risk_emoji} **{action['name']}** - ?????{int(action['success_rate']*100)}% | ????{action['base_reward']} ??n"
        
        embed.add_field(
            name="?? ????????",
            value=actions_text,
            inline=False
        )
        
        embed.set_footer(text=f"{level_info['description']} | ???????????????")
        embed.timestamp = datetime.utcnow()
        
        return embed
    except Exception as e:
        traceback.print_exc()
        return discord.Embed(title="??? ?????????", description="???????????????????????)

def create_level_up_embed(user_obj, old_level, new_level, bonus_coins):
    """????????? Embed"""
    old_info = LEVELS[old_level]
    new_info = LEVELS[new_level]
    
    embed = discord.Embed(
        title="",
        description=(
            "# ?? ???????????????????????????????????????????\n"
            "# ???????????????????\n"
            "# ?? ???????????????????????????????????????????"
        ),
        color=0xFFD700
    )
    
    embed.add_field(
        name="?? ??????",
        value=(
            f"?? **???**??user_obj.mention}\n"
            f"?? **???**??~{old_info['title']}~~ ??**{new_info['title']}**\n"
            f"??**???**??v.{old_level} ??**Lv.{new_level}**\n"
            f"?? **?????*??new_info['salary']:,} KK??????"
        ),
        inline=False
    )
    
    salary_diff = new_info['salary'] - old_info['salary']
    embed.add_field(
        name="?? ???????????",
        value=(
            f"```diff\n"
            f"+ ????????{bonus_coins:,} KK??n"
            f"+ ????????{salary_diff:,} KK????n"
            f"+ ????????len(new_info['actions'])} ??n"
            f"+ ?????????@{new_info['title']}\n"
            f"```"
        ),
        inline=False
    )
    
    # ?????????
    old_action_names = {a['name'] for a in old_info['actions']}
    new_actions = [a for a in new_info['actions'] if a['name'] not in old_action_names]
    
    if new_actions:
        new_actions_text = ""
        for action in new_actions:
            new_actions_text += f"??**{action['name']}** (?????{int(action['success_rate']*100)}%, ????{action['base_reward']} ??\n"
        
        embed.add_field(
            name="?? ????????",
            value=new_actions_text,
            inline=False
        )
    
    level_messages = {
        1: "?? ??????????????????????????????????",
        2: "?? ?????????????????????????????",
        3: "?? ???????????????????????????????",
        4: "?? ??????????????????????????????",
        5: "?? ?????????????????????????????",
        6: "?? ?????????????????????????????????
    }
    
    embed.add_field(
        name="?? ?????????",
        value=f"*??level_messages.get(new_level, '?????????????????????')}??",
        inline=False
    )
    
    embed.set_footer(text="?? KK??? | ???????????????")
    embed.timestamp = datetime.utcnow()
    
    return embed

async def assign_role(guild, user, new_level):
    """?????????????????????????????????"""
    try:
        member = guild.get_member(user.id)
        if not member:
            print(f"??? ?????????{user.id}")
            return False
        
        all_level_roles = [LEVELS[lvl]["role_id"] for lvl in LEVELS.keys() if LEVELS[lvl]["role_id"]]
        
        roles_to_remove = []
        for role in member.roles:
            if role.id in all_level_roles:
                roles_to_remove.append(role)
        
        if roles_to_remove:
            await member.remove_roles(*roles_to_remove, reason="?????????????????)
            print(f"???????{len(roles_to_remove)} ????????)
        
        new_role_id = LEVELS[new_level]["role_id"]
        if new_role_id:
            new_role = guild.get_role(new_role_id)
            if new_role:
                await member.add_roles(new_role, reason=f"?????{LEVELS[new_level]['title']}")
                print(f"?????????????new_role.name}")
                return True
            else:
                print(f"??? ?????????ID??new_role_id}")
        else:
            print(f"??? ??? {new_level} ?????????ID")
        
        return False
    except discord.Forbidden:
        print("????????????????????")
        return False
    except Exception as e:
        print(f"???????????????????e}")
        traceback.print_exc()
        return False

async def process_checkin(user_id, user_obj, guild):
    """????????? - ??? AI ???????????"""
    try:
        user = get_user(user_id)
        
        # ???????????????
        if not user:
            print(f"???????????????? {user_id}")
            return None, None, None, None
        
        today = datetime.utcnow().strftime("%Y-%m-%d")
        yesterday = (datetime.utcnow() - timedelta(days=1)).strftime("%Y-%m-%d")
        
        # ??? #1: ?????? streak - ??????????????????????
        last_work_date = user.get('last_work_date', None)
        
        if last_work_date == yesterday:
            # ??????????????? ??????????
            streak = user.get('streak', 0) + 1
        elif last_work_date is None or last_work_date != today:
            # ?????????????????????????????????
            streak = 1
        else:
            # ?????????????????
            streak = user.get('streak', 0) + 1
        
        level = user.get('level', 0)
        
        # ????????+ ?????????
        xp_gain = random.randint(20, 60)
        xp_boost = LEVELS[level].get("xp_boost", 1.0)
        xp_gain = int(xp_gain * xp_boost)
        
        # ?????? (0-100%) + ?????????
        base_salary = LEVELS[level]["salary"]
        salary_multiplier = random.uniform(0, 1)
        salary_boost = LEVELS[level].get("salary_boost", 1.0)
        kkcoin_gain = int(base_salary * salary_multiplier * salary_boost)

        leveled_up = False
        bonus_coins = 0
        old_level = level
        
        temp_user = user.copy()
        temp_user['streak'] = streak
        temp_user['xp'] = user.get('xp', 0) + xp_gain
        
        can_level_up, _ = check_level_up(temp_user)
        
        if can_level_up and level < 6:
            level += 1
            streak = 0
            bonus_coins = 300 * level
            kkcoin_gain += bonus_coins
            leveled_up = True
            
            role_assigned = await assign_role(guild, user_obj, level)
            if role_assigned:
                print(f"????? {user_obj.name} ?????Lv.{level}???????????")

        # ??? #2: ????????? actions_used - ????????????
        # ??? #2b: ??? last_work_date ???????????????????actions_used
        update_data = {
            'last_work_date': today,
            'streak': streak,
            'level': level,
            'xp': temp_user['xp'],
            'kkcoin': user.get('kkcoin', 0) + kkcoin_gain
        }
        
        # ????????????????????????????????actions_used
        # ??????????????????????????????
        if last_work_date != today:
            update_data['actions_used'] = '{}'
        
        update_user(user_id, **update_data)

        updated_user = get_user(user_id)
        
        # ??? AI ????????????
        daily_story = await generate_daily_checkin_story(
            level_title=LEVELS[updated_user['level']]['title'],
            salary_percent=salary_multiplier,
            streak=streak,
            user_name=user_obj.display_name
        )
        
        if leveled_up:
            level_up_embed = create_level_up_embed(user_obj, old_level, level, bonus_coins)
            work_embed = create_work_embed(updated_user, user_obj)
            return (level_up_embed, work_embed), updated_user, salary_multiplier, daily_story
        else:
            embed = create_work_embed(updated_user, user_obj)
            return (embed,), updated_user, salary_multiplier, daily_story
            
    except Exception as e:
        traceback.print_exc()
        return None, None, None, None

async def generate_daily_checkin_story(level_title, salary_percent, streak, user_name):
    """??? AI ???????????????"""
    try:
        if not AI_API_KEY or not AI_API_URL:
            return get_fallback_checkin_story(salary_percent)
        
        # ???????????????????
        if salary_percent >= 0.8:
            situation = "????????????"
        elif salary_percent >= 0.5:
            situation = "???????????"
        else:
            situation = "??????????????"
        prompt = f"""????????????????????????????????????

????????level_title}
????????{situation}????????? {int(salary_percent*100)}%??
????????streak} ??

????????1-2 ??????????????????????????????????????????
1. ??????????????
2. ??????????????
3. ??????????????
4. ???????????? 50 ??
5. ????????????

???????????????????????????""

        headers = {
            "Authorization": f"Bearer {AI_API_KEY}",
            "Content-Type": "application/json"
        }
        
        data = {
            "model": AI_API_MODEL,
            "messages": [
                {
                    "role": "system",
                    "content": "?????????????????????????????
                },
                {
                    "role": "user",
                    "content": prompt
                }
            ],
            "temperature": 0.7,
            "max_tokens": 150
        }
        
        async with aiohttp.ClientSession() as session:
            async with session.post(AI_API_URL, headers=headers, json=data, timeout=8) as response:
                if response.status == 200:
                    result = await response.json()
                    story = result['choices'][0]['message']['content'].strip()
                    return story.strip('"').strip("'").strip()
                else:
                    return get_fallback_checkin_story(salary_percent)
                    
    except Exception as e:
        print(f"??? AI ????????????: {e}")
        return get_fallback_checkin_story(salary_percent)

def get_fallback_checkin_story(salary_percent):
    """??????????????\"""
    if salary_percent >= 0.8:
        stories = [
            "???????????????????????",
            "????????????????????",
            "????????????????????"
        ]
    elif salary_percent >= 0.5:
        stories = [
            "???????????????????",
            "???????????????????",
            "??????????????????????"
        ]
    else:
        stories = [
            "???????????????????????",
            "??????????????????????",
            "??????????????????????"
        ]
    return random.choice(stories)

# ??????????????
ACTION_STORIES = {
    "???": {
        "success": [
            "???????????????ATM??????????????? {reward} KK?????????????????",
            "???????????????????????{reward} KK?????????????????????????",
            "????????????????? {reward} KK?????????????????????????????"
        ],
        "fail": [
            "ATM?????????????????????????????????????????...",
            "????????????????????????????????????????????????",
            "????????????????????????????????????????????????"
        ]
    },
    "???": {
        "success": [
            "?????????????????{reward} KK??????????????????????????",
            "???????????????????????????????????????????? {reward} KK????",
            "????????????????????reward} KK???????????????????????????"
        ],
        "fail": [
            "??????????????????????????????????????????????????????????",
            "????????????????????????????????????????????",
            "????????????????????????????????????"
        ]
    },
    "????": {
        "success": [
            "??????????????????????????reward} KK???????????????????????",
            "???????????????????? {reward} KK??????????????????????",
            "??????????????????????????? {reward} KK??????????"
        ],
        "fail": [
            "???????????????????????????????????????????",
            "???????????????????????????????????",
            "?????????????????????????????????"
        ]
    },
    "??????": {
        "success": [
            "???????????????????????? 2 ?????????????????{reward} KK????????",
            "????????????????????6 ??????????????????????? {reward} KK????",
            "????????????????????????????????????? {reward} KK????"
        ],
        "fail": [
            "?????????????????????????????????????????????",
            "??????????????????????????????????????",
            "?????????????????????????????????"
        ]
    },
    "???": {
        "success": [
            "??????????????????????reward} KK?????????????????????????",
            "??????????????????????????????????{reward} KK????",
            "?????20 ????????????????????????????reward} KK???????"
        ],
        "fail": [
            "??????????????????????????????????????????",
            "??????????????????????????????????",
            "?????????????????????????????????????"
        ]
    },
    "???": {
        "success": [
            "??????????????????????????????????????reward} KK?????????????",
            "??????????????????????????????????reward} KK????",
            "????????????????????? {reward} KK???????"
        ],
        "fail": [
            "???????????????????????????????????????",
            "??????????????????????????????",
            "?????????????????????????????"
        ]
    },
    "?????": {
        "success": [
            "???????????????????????????????????????reward} KK???????",
            "????????????????????????????????????????????????????{reward} KK????",
            "??????????????????????????reward} KK??????????"
        ],
        "fail": [
            "???????????????????????????????????????",
            "?????????????????????????????????",
            "????????????????????????????????????"
        ]
    },
    "??????": {
        "success": [
            "??????????????????????{reward} KK?????????????????... ???",
            "??????????????{reward} KK??????????????????????????????",
            "????????{reward} KK??????????????????????????????"
        ],
        "fail": [
            "??????????????????????????????????",
            "?????????????????????????????????????",
            "?????????????????????????????"
        ]
    },
    "???": {
        "success": [
            "??????????????????????????????{reward} KK???????????????",
            "??????????????????????????{reward} KK???????????????",
            "?????????????????????????????? {reward} KK????"
        ],
        "fail": [
            "?????????????????????????????????",
            "??????????????????????????????",
            "????????????????????????????????????????"
        ]
    },
    "??????": {
        "success": [
            "??????????24 ????????????????? {reward} KK????",
            "??????????????????????????? {reward} KK????",
            "???????????????????????reward} KK???????"
        ],
        "fail": [
            "????????????????????????",
            "????????????????????????????",
            "??????????????????????????????????"
        ]
    },
    "??????": {
        "success": [
            "??????????????????????????????????{reward} KK???????",
            "????????????????????????????? {reward} KK????",
            "?????????????????????????????????{reward} KK????"
        ],
        "fail": [
            "????????????????????????????",
            "??????????????????????????",
            "??????????????????????????????????????"
        ]
    },
    "??????": {
        "success": [
            "??????????????????????????20%?????{reward} KK????",
            "???????????????????????????????? {reward} KK????",
            "?????????????????????????????? {reward} KK????"
        ],
        "fail": [
            "??????????????????????????????",
            "??????????????????????????????",
            "???????????????????????????"
        ]
    },
    "??????": {
        "success": [
            "??????????????????????????reward} KK???????????????????",
            "?????????????????????????????? {reward} KK????",
            "?????????????????? {reward} KK?????????????"
        ],
        "fail": [
            "??????????????????????????????????????",
            "?????????????????????????????????????",
            "?????????????????????????????????????????"
        ]
    }
}

async def generate_story_with_ai(action_name, level_title, success, reward, user_name):
    """??? AI API ?????????"""
    try:
        if not AI_API_KEY or not AI_API_URL:
            # ????????? AI API???????????
            return get_fallback_story(action_name, success, reward)
        
        # ??????????????????
        result_type = "???" if success else "???"
        
        prompt = f"""????????????????????????????????????????????

????????level_title}
????????action_name}
????????result_type}
{"???????? + str(reward) + " KK?? if success else "????????}

????????2-3 ???????????????????????????????????
1. ??????????????????
2. ????????????????????
3. ??????????????????????????{reward if success else 0} KK??
4. ???????????????????????
5. ???????????? 80 ??
6. ????????????????????

???????????????????????????????""

        headers = {
            "Authorization": f"Bearer {AI_API_KEY}",
            "Content-Type": "application/json"
        }
        
        data = {
            "model": AI_API_MODEL,
            "messages": [
                {
                    "role": "system",
                    "content": "?????????????????????????????????????????????????????????
                },
                {
                    "role": "user",
                    "content": prompt
                }
            ],
            "temperature": 0.8,
            "max_tokens": 200
        }
        
        async with aiohttp.ClientSession() as session:
            async with session.post(AI_API_URL, headers=headers, json=data, timeout=10) as response:
                if response.status == 200:
                    result = await response.json()
                    story = result['choices'][0]['message']['content'].strip()
                    
                    # ??????????????????
                    story = story.strip('"').strip("'").strip()
                    
                    return story
                else:
                    print(f"??? AI API ??????: {response.status}")
                    return get_fallback_story(action_name, success, reward)
                    
    except asyncio.TimeoutError:
        print("??? AI API ??????")
        return get_fallback_story(action_name, success, reward)
    except Exception as e:
        print(f"??? AI ?????????: {e}")
        traceback.print_exc()
        return get_fallback_story(action_name, success, reward)

def get_fallback_story(action_name, success, reward):
    """??AI API ??????????????"""
    if success:
        stories = {
            "???": f"????????????{reward} KK??????????\",
            "???": f"??????????????? {reward} KK????\",
            "????\": f\"???????????reward} KK????????????????????\",
            "??????\": f\"????????????????????{reward} KK????\",
            "???\": f\"???????????reward} KK?????????????\",
            "???\": f\"?????????????????{reward} KK????\",
            "?????\": f\"?????????????????{reward} KK????\",
            "??????\": f\"????????reward} KK??????????????\",
            "???\": f\"?????????????????????? {reward} KK????\",
            "??????\": f\"?????????????? {reward} KK????\",
            "??????\": f\"??????????????{reward} KK???????\",
            "??????\": f\"?????????????????{reward} KK???????\",
            "??????\": f\"????????????????? {reward} KK???\"
        }
        return stories.get(action_name, f"???????????{reward} KK???")
    else:
        stories = {
            "???": "ATM ?????????????????????",
            "???": "???????????????????????",
            "????: "??????????????????????????,
            "??????": "??????????????????????",
            "???": "??????????????????????",
            "???": "??????????????????????",
            "?????: "????????????????????????,
            "??????": "??????????????????????????",
            "???": "??????????????????????",
            "??????": "???????????????????????",
            "??????": "????????????????????",
            "??????": "??????????????????????",
            "??????": "?????????????????????????????
        }
        return stories.get(action_name, "???????????????????)

async def process_work_action(user_id, user_obj, action):
    """????????? - ??? AI ??????"""
    try:
        user = get_user(user_id)
        
        # ???????????????
        if not user:
            print(f"???????????????? {user_id}")
            return None, None, "??????????????"
        
        today = datetime.utcnow().strftime("%Y-%m-%d")
        
        actions_used = json.loads(user.get('actions_used', '{}'))
        if action in actions_used:
            return None, None, "??????????????????????
        
        level = user['level']
        level_info = LEVELS[level]
        
        # ??????????????
        action_data = None
        for act in level_info['actions']:
            if act['name'] == action:
                action_data = act
                break
        
        if not action_data:
            return None, None, "???????????
        
        # ???????????
        success = random.random() < action_data['success_rate']
        
        if success:
            # ????????0-100% ?????
            reward_multiplier = random.uniform(0, 1)
            kk_gain = int(action_data['base_reward'] * reward_multiplier)
            xp_gain = action_data['xp']
            result_color = 0x00ff00  # ???
            result_title = f"??{action} - ???"
        else:
            # ???????????
            kk_gain = 0
            xp_gain = action_data['xp'] // 4  # ?????? 25% ???
            result_color = 0xff0000  # ???
            result_title = f"??{action} - ???"
        
        # ??? AI ??????
        story = await generate_story_with_ai(
            action_name=action,
            level_title=level_info['title'],
            success=success,
            reward=kk_gain,
            user_name=user_obj.display_name
        )
        
        # ????????
        actions_used[action] = today
        update_user(
            user_id, 
            xp=user['xp'] + xp_gain, 
            kkcoin=user['kkcoin'] + kk_gain,
            actions_used=json.dumps(actions_used)
        )
        
        updated_user = get_user(user_id)
        
        # ???????????????
        if not updated_user:
            print(f"??? ???????????? {user_id} ?????)
            return None, None, "??? ?????????"
        
        # ?????? embed
        result_embed = discord.Embed(
            title=result_title,
            description=f"*{story}*",
            color=result_color
        )
        
        result_embed.add_field(
            name="?? ??????",
            value=(
                f"**?????*??int(action_data['success_rate']*100)}%\n"
                f"**??????**??'?? ?? if action_data['risk'] <= 0.2 else '?? ?? if action_data['risk'] <= 0.4 else '?? ??}\n"
                f"**???????*??action_data['base_reward']:,} KK??
            ),
            inline=True
        )
        
        result_embed.add_field(
            name="?? ??????",
            value=(
                f"**??????**??kk_gain:,} KK??n"
                f"**??????**??{xp_gain} XP\n"
                f"**??????**??updated_user['kkcoin']:,} KK??
            ),
            inline=True
        )
        
        result_embed.set_footer(text=f"??AI ?????? | {user_obj.name}")
        result_embed.timestamp = datetime.utcnow()
        
        # ????????????
        level_up_message = None
        can_level_up, info = check_level_up(updated_user)
        
        if can_level_up:
            next_salary = LEVELS[updated_user['level'] + 1]["salary"]
            level_up_message = f"?? **????????????**\n???????????????????????**{next_salary:,} KK??*??
        elif updated_user['level'] < 5:
            missing_days = info['required_days'] - info['current_days']
            missing_xp = info['required_xp'] - info['current_xp']
            missing_weeks = missing_days // 7
            
            if not info['days_met'] and not info['xp_met']:
                level_up_message = f"?? ????????????????? **{missing_days}** ??({missing_weeks} ?? ??**{missing_xp:,}** XP"
            elif not info['xp_met']:
                level_up_message = f"?? ?????????????????**{missing_xp:,}** XP ??????"
            elif not info['days_met']:
                level_up_message = f"?????????????????????? **{missing_days}** ??({missing_weeks} ?? ??????"
        
        # ???????????
        work_embed = create_work_embed(updated_user, user_obj)
        
        return (result_embed, work_embed), updated_user, level_up_message
    except Exception as e:
        traceback.print_exc()
        return None, None, "???????????????"

async def setup(bot):
    """??????????????????"""
    print("Work system utilities loaded successfully!")
