# DISABLED: Codex Consensus Review
# This workflow has been disabled to unblock Claude from waiting for Codex responses.
# To re-enable: restore the original content from git history.
# Original content backed up in git history.

name: Codex Consensus Review (DISABLED)

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [main]

jobs:
  codex-review:
    name: Codex Deep Review (DISABLED)
    runs-on: ubuntu-latest
    timeout-minutes: 1
    if: false  # Disabled - never runs
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. See file header for details."
